{
  "name": "emoカレ-by-kosei-kotani",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "setup": "bash install.sh",
    "check-env": "node -e \"const fs=require('fs'); if(!fs.existsSync('.env')) { console.log('⚠️  .envファイルが存在しません。.env.templateを参考に作成してください。'); process.exit(1); } else { console.log('✓ .envファイルが存在します'); }\""
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "lucide-react": "^0.562.0",
    "@google/genai": "^1.35.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
